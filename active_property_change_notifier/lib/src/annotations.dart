class NotifyPropertyChange{
  const NotifyPropertyChange();
}

class CustomField{
  final Type type;

  const CustomField(this.type);
}

const propagateChanges = PropagateChanges();

class PropagateChanges{
  const PropagateChanges();
}
